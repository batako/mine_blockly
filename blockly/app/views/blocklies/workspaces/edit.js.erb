$("#workspace_form")
  .html('<%= j( render "form", workspace: @workspace.mine? ? @workspace : Workspace.new ) %>');

Blockly.Xml.appendDomToWorkspace(
  Blockly.Xml.textToDom('<%= @workspace.xml.html_safe %>'),
  Code.workspace
);
