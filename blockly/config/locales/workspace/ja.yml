ja:
  activerecord:
    models:
      workspace: 作業空間
    attributes:
      workspace:
        id:         ID
        created_by: 作成者
        lock:       ロック
        name:       名前
        pin:        ピン
        xml:        ブロック
        created_at: 作成日時
        updated_at: 更新日時
    errors:
      models:
        workspace:
          attributes:
            xml:
              lock_workspace_error: はロックしているため更新できません
